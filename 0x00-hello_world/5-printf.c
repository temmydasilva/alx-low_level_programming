#include <stdio.h>

/**
 * main - It is the function where the program’s execution starts
 *
 * Return: 0 when succesful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
