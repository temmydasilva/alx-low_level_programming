#include <stdio.h>

/**
 * main - this is the main function of this task
 * Return: always 0
 */

int main(void)

{
	char (i);
	for (i = 'a'; i <= 'z'; i++)
		if (i != 'q' && i != 'e')
			putchar(0);
	putchar('\n');
		return (0);
}
